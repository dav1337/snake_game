# 🐍 Snake Game Alpha 0.1

A classic Snake game implemented in C++ for the purpose of verification my bad skills in programming
In future I maybe update this ugly sh*t game idkkkk ya typoi

## ✨ Features
- Very basic Snake gameplay
- Dynamic field rendering using `ncurses`

## 🎮 Controls (Only ENG layout work)
- `W` - Move Up
- `S` - Move Down
- `A` - Move Left
- `D` - Move Right

## ⚙️ Installation & Setup

### Prerequisites
You need to have the following installed:
- **C++17 compatible compiler** (usually `g++` or `clang++`)
- **CMake** (version 3.12 or higher)
- **ncurses library**

### Installing Dependencies

#### Ubuntu/Debian:
```bash
sudo apt update
sudo apt install build-essential cmake libncurses5-dev libncursesw5-dev git
```

#### Arch Linux:
```bash
sudo pacman -S gcc cmake ncurses git
```

## 🚀 How to Run

### Using Makefile:
```bash
git clone https://github.com/d1337e/snake_game.git
cd snake_game
make
make run
```

## To do
- **Add interactive menu**
- **Add a primitive point system**
- **Make the "project haha" cross-platform**
